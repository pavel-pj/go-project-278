:80

# Статика из /app/public
root * /app/public

# Маршруты API проксируем на бэкенд
handle /api/* {
    reverse_proxy localhost:8080
}

handle /ping {
    reverse_proxy localhost:8080
}

# Всё остальное (включая /assets/*) — статика
handle {
    file_server
}